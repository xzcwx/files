import * as files from "./lib/files";

const {
  downloadFile,
  downloadFileSync,
  loads,
  H,
  http,
  setToken,
  MIME,
  FILE_TYPE,
  FileConfig,
  FileResponse
} = files;

export {
  files as default,
  downloadFile,
  downloadFileSync,
  loads,
  H,
  http,
  setToken,
  MIME,
  FILE_TYPE,
  FileConfig,
  FileResponse
};
